--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--!nolint LocalUnused
--!nolint FunctionUnused
--#selene: allow(shadowing)
-- File generated by Blink v0.0.0 (https://github.com/XopoIII/blink)
-- This file is not meant to be edited

_G._BLINK = _G._BLINK or {}
if _G._BLINK["BLINK"] then
	error("[Blink]: An instance of blink is already running with the remote scope \"BLINK\". Change the remote scope of either instance to avoid conflicts.")
end
_G._BLINK["BLINK"] = true
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local SerdesFunctions = {}
local BASE_EVENT_NAME = "BLINK"
local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

local Null = newproxy()

type Entry = {
    value: any,
    next: Entry?
}

type Queue = {
    head: Entry?,
    tail: Entry?
}

type BufferSave = {
    Size: number, 
    Cursor: number, 
    Buffer: buffer, 
    Instances: {Instance}
}

local function Read(Bytes: number)
    local Offset = RecieveCursor
    RecieveCursor += Bytes
    return Offset
end

local function Save(): BufferSave
    return {
        Size = SendSize,
        Cursor = SendCursor,
        Buffer = SendBuffer,
        Instances = SendInstances
    }
end

local function Load(Save: BufferSave?)
    if Save then
        SendSize = Save.Size
        SendCursor = Save.Cursor
        SendOffset = Save.Cursor
        SendBuffer = Save.Buffer
        SendInstances = Save.Instances
        return
    end

    SendSize = 64
    SendCursor = 0
    SendOffset = 0
    SendBuffer = buffer.create(64)
    SendInstances = {}
end

local function Invoke()
    if Invocations == 255 then
        Invocations = 0
    end

    local Invocation = Invocations
    Invocations += 1
    return Invocation
end

local function Allocate(Bytes: number)
    local InUse = (SendCursor + Bytes)
    if InUse > SendSize then
        --> Avoid resizing the buffer for every write
        while InUse > SendSize do
            SendSize *= 1.5
        end

        local Buffer = buffer.create(SendSize)
        buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
        SendBuffer = Buffer
    end

    SendOffset = SendCursor
    SendCursor += Bytes
    
    return SendOffset
end

local function CreateQueue(): Queue
    return {
        head = nil,
        tail = nil
    }
end

local function Pop(queue: Queue): any
    local head = queue.head
    if head == nil then
        return
    end

    queue.head = head.next
    return head.value
end

local function Push(queue: Queue, value: any)
    local entry: Entry = {
        value = value,
        next = nil
    }

    if queue.tail ~= nil then
        queue.tail.next = entry
    end

    queue.tail = entry

    if queue.head == nil then
        queue.head = entry
    end
end

local Calls = table.create(256)

local Events: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local Queue: any = {
    Reliable = table.create(256),
    Unreliable = table.create(256)
}

local SerdesEvent = "?"
local SerdesThread: thread?
Queue.Unreliable[0] = table.create(256)
Queue.Unreliable[1] = table.create(256)
export type Generics_a = { Type: "Header", Sequence: number, Fragments: number } | { Type: "Fragment", Fragment: number, Fragments: number }
export type Generics_GenericMap<Key,Value> = {[Key]: Value}
export type Generics_GenericEnum<T,G> = { Type: "A", Data: T } | { Type: "B", Data: G }
export type Generics_Map = {[string]: number}
export type Generics_b = { Type: "A", Data: string } | { Type: "B", Data: number }
export type Generics_Packet<T> = { Sequence: number, Ack: number, Data: { Type: "Single", Data: T } | { Type: "Fragment", Fragment: number, Fragments: number, Data: T } }
export type Generics_Entity = { id: number, pos: Vector3, angle: number }
export type Example_a = { Type: "Header", Sequence: number, Fragments: number } | { Type: "Fragment", Fragment: number, Fragments: number }
export type Example_GenericMap<Key,Value> = {[Key]: Value}
export type Example_GenericEnum<T,G> = { Type: "A", Data: T } | { Type: "B", Data: G }
export type Example_Map = {[string]: number}
export type Example_b = { Type: "A", Data: string } | { Type: "B", Data: number }
export type Example_Packet<T> = { Sequence: number, Ack: number, Data: { Type: "Single", Data: T } | { Type: "Fragment", Fragment: number, Fragments: number, Data: T } }
export type Example_Entity = { id: number, pos: Vector3, angle: number }
export type Source_Source2_a = number
export type Source_a = number
export type Source2_a = number
function SerdesFunctions.Generics_ReadEVENT_Snapshot(): ({ Sequence: number, Ack: number, Data: { Type: "Single", Data: { id: number, pos: Vector3, angle: number } } | { Type: "Fragment", Fragment: number, Fragments: number, Data: { id: number, pos: Vector3, angle: number } } })
	-- Read BLOCK: 5 bytes
	local BLOCK_START = Read(5)
	-- Value: Snapshot
	local Value = {} :: any
	-- Value.Sequence: Sequence
	Value.Sequence = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	-- Value.Ack: Ack
	Value.Ack = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	-- Value.Data: Data
	local Tag = buffer.readu8(RecieveBuffer, BLOCK_START + 4)
	Value.Data = true :: any
	if Tag == 0 then
		-- Read BLOCK: 15 bytes
		local BLOCK_START = Read(15)
		-- Value.Data: Single
		Value.Data = {} :: any
		-- Value.Data.Data: Data
		Value.Data.Data = {} :: any
		-- Value.Data.Data.id: id
		Value.Data.Data.id = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		-- Value.Data.Data.pos: pos
		local X = buffer.readf32(RecieveBuffer, BLOCK_START + 1)
		local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 5)
		local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 9)
		Value.Data.Data.pos = Vector3.new(X, Y, Z)
		-- Value.Data.Data.angle: angle
		Value.Data.Data.angle = buffer.readu16(RecieveBuffer, BLOCK_START + 13)
		Value.Data.Type = "Single"
	elseif Tag == 1 then
		-- Read BLOCK: 17 bytes
		local BLOCK_START = Read(17)
		-- Value.Data: Fragment
		Value.Data = {} :: any
		-- Value.Data.Fragment: Fragment
		Value.Data.Fragment = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		-- Value.Data.Fragments: Fragments
		Value.Data.Fragments = buffer.readu8(RecieveBuffer, BLOCK_START + 1)
		-- Value.Data.Data: Data
		Value.Data.Data = {} :: any
		-- Value.Data.Data.id: id
		Value.Data.Data.id = buffer.readu8(RecieveBuffer, BLOCK_START + 2)
		-- Value.Data.Data.pos: pos
		local X = buffer.readf32(RecieveBuffer, BLOCK_START + 3)
		local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 7)
		local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 11)
		Value.Data.Data.pos = Vector3.new(X, Y, Z)
		-- Value.Data.Data.angle: angle
		Value.Data.Data.angle = buffer.readu16(RecieveBuffer, BLOCK_START + 15)
		Value.Data.Type = "Fragment"
	else
		error(`Unexpected variant: {Tag}`)
	end
	return Value
end

function SerdesFunctions.Example_ReadEVENT_Snapshot(): ({ Sequence: number, Ack: number, Data: { Type: "Single", Data: { id: number, pos: Vector3, angle: number } } | { Type: "Fragment", Fragment: number, Fragments: number, Data: { id: number, pos: Vector3, angle: number } } })
	-- Read BLOCK: 5 bytes
	local BLOCK_START = Read(5)
	-- Value: Snapshot
	local Value = {} :: any
	-- Value.Sequence: Sequence
	Value.Sequence = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	-- Value.Ack: Ack
	Value.Ack = buffer.readu16(RecieveBuffer, BLOCK_START + 2)
	-- Value.Data: Data
	local Tag = buffer.readu8(RecieveBuffer, BLOCK_START + 4)
	Value.Data = true :: any
	if Tag == 0 then
		-- Read BLOCK: 15 bytes
		local BLOCK_START = Read(15)
		-- Value.Data: Single
		Value.Data = {} :: any
		-- Value.Data.Data: Data
		Value.Data.Data = {} :: any
		-- Value.Data.Data.id: id
		Value.Data.Data.id = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		-- Value.Data.Data.pos: pos
		local X = buffer.readf32(RecieveBuffer, BLOCK_START + 1)
		local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 5)
		local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 9)
		Value.Data.Data.pos = Vector3.new(X, Y, Z)
		-- Value.Data.Data.angle: angle
		Value.Data.Data.angle = buffer.readu16(RecieveBuffer, BLOCK_START + 13)
		Value.Data.Type = "Single"
	elseif Tag == 1 then
		-- Read BLOCK: 17 bytes
		local BLOCK_START = Read(17)
		-- Value.Data: Fragment
		Value.Data = {} :: any
		-- Value.Data.Fragment: Fragment
		Value.Data.Fragment = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		-- Value.Data.Fragments: Fragments
		Value.Data.Fragments = buffer.readu8(RecieveBuffer, BLOCK_START + 1)
		-- Value.Data.Data: Data
		Value.Data.Data = {} :: any
		-- Value.Data.Data.id: id
		Value.Data.Data.id = buffer.readu8(RecieveBuffer, BLOCK_START + 2)
		-- Value.Data.Data.pos: pos
		local X = buffer.readf32(RecieveBuffer, BLOCK_START + 3)
		local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 7)
		local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 11)
		Value.Data.Data.pos = Vector3.new(X, Y, Z)
		-- Value.Data.Data.angle: angle
		Value.Data.Data.angle = buffer.readu16(RecieveBuffer, BLOCK_START + 15)
		Value.Data.Type = "Fragment"
	else
		error(`Unexpected variant: {Tag}`)
	end
	return Value
end

if not RunService:IsRunning() then
	local NOOP = function() end
	local NOOP_RETURNING = function() return NOOP end
	local Returns = table.freeze({
		StepReplication = NOOP,
	Generics = {
		Entity = {
			Read = function(Buffer: buffer): (Generics_Entity)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 15 bytes
				local BLOCK_START = Read(15)
				-- Buffer read
				-- Value: Entity
				local Value = {} :: any
				-- Value.id: id
				Value.id = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				-- Value.pos: pos
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 1)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 5)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 9)
				Value.pos = Vector3.new(X, Y, Z)
				-- Value.angle: angle
				Value.angle = buffer.readu16(RecieveBuffer, BLOCK_START + 13)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: Generics_Entity): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 15 bytes
				local BLOCK_START = Allocate(15)
				-- Buffer write
				-- Value: Entity
				-- Value.id: id
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.id)
				-- Value.pos: pos
				local Vector = Value.pos
				buffer.writef32(SendBuffer, BLOCK_START + 1, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 5, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 9, Vector.Z)
				-- Value.angle: angle
				buffer.writeu16(SendBuffer, BLOCK_START + 13, Value.angle)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Snapshot = {
			On = NOOP_RETURNING
		},
	},
	Example = {
		Entity = {
			Read = function(Buffer: buffer): (Example_Entity)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 15 bytes
				local BLOCK_START = Read(15)
				-- Buffer read
				-- Value: Entity
				local Value = {} :: any
				-- Value.id: id
				Value.id = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				-- Value.pos: pos
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 1)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 5)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 9)
				Value.pos = Vector3.new(X, Y, Z)
				-- Value.angle: angle
				Value.angle = buffer.readu16(RecieveBuffer, BLOCK_START + 13)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: Example_Entity): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 15 bytes
				local BLOCK_START = Allocate(15)
				-- Buffer write
				-- Value: Entity
				-- Value.id: id
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.id)
				-- Value.pos: pos
				local Vector = Value.pos
				buffer.writef32(SendBuffer, BLOCK_START + 1, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 5, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 9, Vector.Z)
				-- Value.angle: angle
				buffer.writeu16(SendBuffer, BLOCK_START + 13, Value.angle)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Snapshot = {
			On = NOOP_RETURNING
		},
	},
	Source = {
		Source2 = {
		},
	},
	Source2 = {
	},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsServer() then
    error("Server network module can only be required from the server.")
end

local Reliable: RemoteEvent = ReplicatedStorage:FindFirstChild(BASE_EVENT_NAME .. "_RELIABLE_REMOTE") :: RemoteEvent
if not Reliable then
    local RemoteEvent = Instance.new("RemoteEvent")
    RemoteEvent.Name = BASE_EVENT_NAME .. "_RELIABLE_REMOTE"
    RemoteEvent.Parent = ReplicatedStorage
    Reliable = RemoteEvent
end

local Unreliable: UnreliableRemoteEvent = ReplicatedStorage:FindFirstChild(BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent
if not Unreliable then
    local UnreliableRemoteEvent = Instance.new("UnreliableRemoteEvent")
    UnreliableRemoteEvent.Name = BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE"
    UnreliableRemoteEvent.Parent = ReplicatedStorage
    Unreliable = UnreliableRemoteEvent
end

local PlayersMap: {[Player]: BufferSave} = {}

Players.PlayerRemoving:Connect(function(Player)
    PlayersMap[Player] = nil
end)

--> Writing a player's buffer back is only safe while that player is still in the game.
--> A listener can yield between Load and Save -- a datastore call, a task.wait, any async work --
--> and the player may leave inside that window. PlayerRemoving has already cleared their entry by
--> then, so a plain `PlayersMap[Player] = Save()` puts back an entry that nothing will ever remove
--> again: StepReplication would allocate a buffer and fire a remote at a player who is gone, on
--> every Heartbeat, for the lifetime of the server.
local function SavePlayerBuffer(Player: Player)
    if not Player:IsDescendantOf(Players) then
        return
    end

    PlayersMap[Player] = Save()
end

local function StepReplication()
    for Player, Send in PlayersMap do
        --> Second line of defence. An entry can still be stale if it was written inside the narrow
        --> window of PlayerRemoving itself, before the player was detached; dropping it here makes
        --> the leak self-healing instead of permanent.
        if not Player:IsDescendantOf(Players) then
            PlayersMap[Player] = nil
            continue
        end

        if Send.Cursor <= 0 then
            continue
        end

        local Buffer = buffer.create(Send.Cursor)
        buffer.copy(Buffer, 0, Send.Buffer, 0, Send.Cursor)
        Reliable:FireClient(Player, Buffer, Send.Instances)

        Send.Size = 64
        Send.Cursor = 0
        Send.Buffer = buffer.create(64)
        table.clear(Send.Instances)
    end
end
RunService.Heartbeat:Connect(StepReplication)
Reliable.OnServerEvent:Connect(function(Player: Player, Buffer: buffer, Instances: {Instance})
	if buffer.len(Buffer) > 8192 then
		warn(`[Blink]: Dropped an oversized packet from {Player.Name}.`)
		return
	end
	if #Instances > 256 then
		warn(`[Blink]: Dropped a packet with too many instances from {Player.Name}.`)
		return
	end
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	local DecodedEvents = 0
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		DecodedEvents += 1
		if DecodedEvents > 64 then
			warn(`[Blink]: Dropped the rest of a packet from {Player.Name}, too many events.`)
			break
		end
		local Parsed = pcall(function()
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		end)
		if not Parsed then break end
	end
end)
Unreliable.OnServerEvent:Connect(function(Player: Player, Buffer: buffer, Instances: {Instance})
	if buffer.len(Buffer) > 8192 then
		warn(`[Blink]: Dropped an oversized packet from {Player.Name}.`)
		return
	end
	if #Instances > 256 then
		warn(`[Blink]: Dropped a packet with too many instances from {Player.Name}.`)
		return
	end
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	local DecodedEvents = 0
	while (RecieveCursor < Size) do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		DecodedEvents += 1
		if DecodedEvents > 64 then
			warn(`[Blink]: Dropped the rest of a packet from {Player.Name}, too many events.`)
			break
		end
		local Parsed = pcall(function()
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 0 then
			local Value: { Sequence: number, Ack: number, Data: { Type: "Single", Data: { id: number, pos: Vector3, angle: number } } | { Type: "Fragment", Fragment: number, Fragments: number, Data: { id: number, pos: Vector3, angle: number } } } = SerdesFunctions.Generics_ReadEVENT_Snapshot()
			if Events.Unreliable[0] ~= nil then
				SerdesEvent = "Snapshot"
				Events.Unreliable[0](Player, Value)
			end
		elseif Index == 1 then
			local Value: { Sequence: number, Ack: number, Data: { Type: "Single", Data: { id: number, pos: Vector3, angle: number } } | { Type: "Fragment", Fragment: number, Fragments: number, Data: { id: number, pos: Vector3, angle: number } } } = SerdesFunctions.Example_ReadEVENT_Snapshot()
			if Events.Unreliable[1] ~= nil then
				SerdesEvent = "Snapshot"
				Events.Unreliable[1](Player, Value)
			end
		else
			error("[Blink]: Unknown event index.")
		end
		end)
		if not Parsed then break end
	end
end)


local Returns = table.freeze({
	StepReplication = StepReplication,

	Generics = {
		Entity = {
			Read = function(Buffer: buffer): (Generics_Entity)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 15 bytes
				local BLOCK_START = Read(15)
				-- Buffer read
				-- Value: Entity
				local Value = {} :: any
				-- Value.id: id
				Value.id = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				-- Value.pos: pos
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 1)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 5)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 9)
				Value.pos = Vector3.new(X, Y, Z)
				-- Value.angle: angle
				Value.angle = buffer.readu16(RecieveBuffer, BLOCK_START + 13)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: Generics_Entity): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 15 bytes
				local BLOCK_START = Allocate(15)
				-- Buffer write
				-- Value: Entity
				-- Value.id: id
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.id)
				-- Value.pos: pos
				local Vector = Value.pos
				buffer.writef32(SendBuffer, BLOCK_START + 1, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 5, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 9, Vector.Z)
				-- Value.angle: angle
				buffer.writeu16(SendBuffer, BLOCK_START + 13, Value.angle)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Snapshot = {
			On = function(Listener: (Player: Player, Value: { Sequence: number, Ack: number, Data: { Type: "Single", Data: { id: number, pos: Vector3, angle: number } } | { Type: "Fragment", Fragment: number, Fragments: number, Data: { id: number, pos: Vector3, angle: number } } }) -> ()): () -> ()
				Events.Unreliable[0] = Listener
				return function (): ()
					if Events.Unreliable[0] == Listener then
						Events.Unreliable[0] = nil
					end
				end
			end
		},
	},
	Example = {
		Entity = {
			Read = function(Buffer: buffer): (Example_Entity)
				-- Buffer overwrite
				local PreviousCursor = RecieveCursor
				local PreviousBuffer = RecieveBuffer
				local PreviousInstanceCursor = RecieveInstanceCursor
				RecieveCursor = 0
				RecieveBuffer = Buffer
				-- Read BLOCK: 15 bytes
				local BLOCK_START = Read(15)
				-- Buffer read
				-- Value: Entity
				local Value = {} :: any
				-- Value.id: id
				Value.id = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
				-- Value.pos: pos
				local X = buffer.readf32(RecieveBuffer, BLOCK_START + 1)
				local Y = buffer.readf32(RecieveBuffer, BLOCK_START + 5)
				local Z = buffer.readf32(RecieveBuffer, BLOCK_START + 9)
				Value.pos = Vector3.new(X, Y, Z)
				-- Value.angle: angle
				Value.angle = buffer.readu16(RecieveBuffer, BLOCK_START + 13)
				-- Buffer restore
				RecieveCursor = PreviousCursor
				RecieveBuffer = PreviousBuffer
				RecieveInstanceCursor = PreviousInstanceCursor
				return Value
			end,
			Write = function(Value: Example_Entity): (buffer)
				-- Buffer overwrite
				local Previous = Save()
				Load()
				-- Allocate BLOCK: 15 bytes
				local BLOCK_START = Allocate(15)
				-- Buffer write
				-- Value: Entity
				-- Value.id: id
				buffer.writeu8(SendBuffer, BLOCK_START + 0, Value.id)
				-- Value.pos: pos
				local Vector = Value.pos
				buffer.writef32(SendBuffer, BLOCK_START + 1, Vector.X)
				buffer.writef32(SendBuffer, BLOCK_START + 5, Vector.Y)
				buffer.writef32(SendBuffer, BLOCK_START + 9, Vector.Z)
				-- Value.angle: angle
				buffer.writeu16(SendBuffer, BLOCK_START + 13, Value.angle)
				-- Buffer restore
				local Result = buffer.create(SendCursor)
				buffer.copy(Result, 0, SendBuffer, 0, SendCursor)
				Load(Previous)
				return Result
			end
		},
		Snapshot = {
			On = function(Listener: (Player: Player, Value: { Sequence: number, Ack: number, Data: { Type: "Single", Data: { id: number, pos: Vector3, angle: number } } | { Type: "Fragment", Fragment: number, Fragments: number, Data: { id: number, pos: Vector3, angle: number } } }) -> ()): () -> ()
				Events.Unreliable[1] = Listener
				return function (): ()
					if Events.Unreliable[1] == Listener then
						Events.Unreliable[1] = nil
					end
				end
			end
		},
	},
	Source = {
		Source2 = {
		},
	},
	Source2 = {
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL