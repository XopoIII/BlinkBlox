--> A bench is a pool of payloads, fired in turn. This one is a single payload, the same every time:
--> the original methodology, kept so the numbers stay comparable with earlier runs.
return { table.create(1000, true) }
